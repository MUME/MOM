Attribute VB_Name = "filter"
Option Explicit
Public theCommand

Public Function dataFilter(ByRef data As String)

   dataFilter = False
'   Debug.Print data
'   Debug.Print "========================================"
'   For n = LBound(theCommand) To UBound(theCommand)
'      Debug.Print theCommand(n)
'   Debug.Print "===" & Len(theCommand(n)) & "===" & LBound(theCommand) & ", " & UBound(theCommand)
'   Next
'   Exit Function

End Function
